server:
  port: "8081" # 生产环境容器内运行在 8081

db:
  # 容器内访问宿主机数据库，必须用 172.17.0.1 (Docker网桥IP)
  dsn: "root:123456@tcp(172.17.0.1:3306)/smart_community?charset=utf8mb4&parseTime=True&loc=Local"

redis:
  # 容器内访问宿主机 Redis
  addr: "172.17.0.1:6379"

minio:
  endpoint: "43.138.85.114:9000"           # 公网IP (供前端访问)
  access_key: "minioadmin"
  secret_key: "minioadmin123"
  bucket: "community"
  use_ssl: false